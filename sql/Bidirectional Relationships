SELECT *
FROM (
    -- Side A: Person -> Contact
    SELECT
            1 AS SIDE,
            LEAST(PCR.PERSON_ID, PCR.CONTACT_PERSON_ID) || '-' || GREATEST(PCR.PERSON_ID, PCR.CONTACT_PERSON_ID) AS PAIR_GROUP,
            PCR.PERSON_ID,
            PAPF.EMPLOYEE_NUMBER,
            PCR.CONTACT_PERSON_ID,
            PCR.CONTACT_TYPE,
            LKUP.MEANING AS RELATIONSHIP,
            PCR.DATE_START,
            PCR.DATE_END,
            PCR.PRIMARY_CONTACT_FLAG,
            PCR.PERSONAL_FLAG,
            PCR.DEPENDENT_FLAG,
            PCR.RLTD_PER_RSDS_W_DSGNTR_FLAG,
            PCR.THIRD_PARTY_PAY_FLAG,
            PCR.BONDHOLDER_FLAG,
            PCR.BENEFICIARY_FLAG
    FROM DQ_PER_CONTACT_RELATIONSHIPS_V PCR
    LEFT JOIN APPS.PER_ALL_PEOPLE_F PAPF ON PAPF.PERSON_ID = PCR.PERSON_ID AND TRUNC(SYSDATE) BETWEEN PAPF.EFFECTIVE_START_DATE AND PAPF.EFFECTIVE_END_DATE
    LEFT JOIN APPS.FND_LOOKUP_VALUES LKUP ON LKUP.LOOKUP_CODE = PCR.CONTACT_TYPE
                                          AND LKUP.LOOKUP_TYPE = 'CONTACT'
                                          AND LKUP.ZD_EDITION_NAME = 'SET1'
    WHERE EXISTS (SELECT 1 FROM DQ_CLEANSE_RESULTS R
                  WHERE R.START_VALUE = PCR.PERSON_ID
                  AND PROCEDURE_NAME = 'DQ_EXISTS'
                  AND FIELD_NAME = 'DQ_PERSON_ID'
                  AND BATCH_SEQ = (SELECT MAX(BATCH_SEQ) FROM DQ_CLEANSE_RESULTS WHERE DOMAIN_NAME = 'HR'))
      AND EXISTS (SELECT 1 FROM DQ_PER_CONTACT_RELATIONSHIPS_V REV
                  WHERE REV.PERSON_ID = PCR.CONTACT_PERSON_ID
                  AND REV.CONTACT_PERSON_ID = PCR.PERSON_ID)

    UNION ALL

    -- Side B: Contact -> Person (the reverse record)
    SELECT
            2 AS SIDE,
            LEAST(PCR2.PERSON_ID, PCR2.CONTACT_PERSON_ID) || '-' || GREATEST(PCR2.PERSON_ID, PCR2.CONTACT_PERSON_ID) AS PAIR_GROUP,
            PCR2.PERSON_ID,
            PAPF2.EMPLOYEE_NUMBER,
            PCR2.CONTACT_PERSON_ID,
            PCR2.CONTACT_TYPE,
            LKUP2.MEANING AS RELATIONSHIP,
            PCR2.DATE_START,
            PCR2.DATE_END,
            PCR2.PRIMARY_CONTACT_FLAG,
            PCR2.PERSONAL_FLAG,
            PCR2.DEPENDENT_FLAG,
            PCR2.RLTD_PER_RSDS_W_DSGNTR_FLAG,
            PCR2.THIRD_PARTY_PAY_FLAG,
            PCR2.BONDHOLDER_FLAG,
            PCR2.BENEFICIARY_FLAG
    FROM DQ_PER_CONTACT_RELATIONSHIPS_V PCR
    JOIN DQ_PER_CONTACT_RELATIONSHIPS_V PCR2 ON PCR2.PERSON_ID = PCR.CONTACT_PERSON_ID
                                              AND PCR2.CONTACT_PERSON_ID = PCR.PERSON_ID
    LEFT JOIN APPS.PER_ALL_PEOPLE_F PAPF2 ON PAPF2.PERSON_ID = PCR2.PERSON_ID AND TRUNC(SYSDATE) BETWEEN PAPF2.EFFECTIVE_START_DATE AND PAPF2.EFFECTIVE_END_DATE
    LEFT JOIN APPS.FND_LOOKUP_VALUES LKUP2 ON LKUP2.LOOKUP_CODE = PCR2.CONTACT_TYPE
                                            AND LKUP2.LOOKUP_TYPE = 'CONTACT'
                                            AND LKUP2.ZD_EDITION_NAME = 'SET1'
    WHERE EXISTS (SELECT 1 FROM DQ_CLEANSE_RESULTS R
                  WHERE R.START_VALUE = PCR.PERSON_ID
                  AND PROCEDURE_NAME = 'DQ_EXISTS'
                  AND FIELD_NAME = 'DQ_PERSON_ID'
                  AND BATCH_SEQ = (SELECT MAX(BATCH_SEQ) FROM DQ_CLEANSE_RESULTS WHERE DOMAIN_NAME = 'HR'))
)
ORDER BY PAIR_GROUP, SIDE;
